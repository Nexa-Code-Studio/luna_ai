# FastAPI Application Server

Primary REST and WebSocket application backend responsible for user authentication, call sessions, conversation state, database interactions, background job dispatching, and mobile client APIs.
